# QA — P2 overlay life / console glyphs, lead live check (23.08.2026, late)

Workflow `kenopsia-p2-sequential` (one agent at a time): package A `e455826` (#16 #17), package B
`86c3ea2` (#18); both refute-first reviews returned minors only — three applied in `a9542ae` (dither
honours CRT off, idle loop writes only on change, ButtonB close respects `processed`). Lead change
after the first live look: band + blip breathe on every lobby screen (`LIVE_SCREENS` Selection/Info/
Status/Score), bracket + UPTIME stay Selection-only (`selectionOk`). Pushed with parity: FeelConfig
12076/743440417, Glyphs (new) 10277/866462064, MachineLayout 24161/1063438768, UiFx 22218/1254385614,
KenopsiaClient 99542/1969622182. Place: `SettingsPanel.Row_SHAKE` cloned from Row_CRT at (18, 356),
panel 492 px.

Measured (desktop, solo):
* Overlay holds Scanlines (Pixelated), vignette ×4, RefreshBand, Blip, Dither (Pixelated), IdleBracket,
  Uptime; `SoundService.KenopsiaUI` SoundGroup + `CrtHum` exist; 7 glow echo labels built.
* Roulette capture: corner-tick bracket around the three tiles, `UPTIME 00:00:14` bottom-left,
  crosshair lock — alive, nothing on the Info screen but band + blip.
* Dither on every `hideAll()` swap: DitherSeq bump → tiles 72454964162888 → 93726535183476 →
  121598984043867 → 128764657994112 at 0.09–0.10 s steps, 0.38 s total, then hidden (four swaps
  logged: briefing, round card, hide, announce).
* Row_SHAKE present, `KenopsiaReduceShake=false` seeded from ReducedMotion.
* Hum: placeholder id 0 (no asset yet — P6), one warn.

Open for the device pass (#20, user): phone MicroProfiler during a dither swap; Xbox emulation +
gamepad walk Info → Settings → Briefing without focus escape; glyph images on console; iPad Tablet
branch. Gate deviation signed off: glyph ImageLabels keep the default ResampleMode (button art).
