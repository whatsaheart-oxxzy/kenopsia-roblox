# QA — Perf pass 2 (#50), 23.08.2026 (late)

Server core and client hot paths from the pass-1 triage; offline gates green (10 suites), selene
clean (3 known client errors, 9 pre-existing warnings), luau-lsp 0 new findings.

| Item | Change |
|---|---|
| 15 `OnServerEvent` handlers on TrialInput | ONE dispatcher + `inputSpecs[trialId]` (TrialKit.wireInput); per-trial specs replaceable, connection made once |
| `livingOf` rebuilt per player per tick | `Round:living()` cache, 0.1 s TTL; `place()` seeds it; the 5 by-uid call sites use it |
| watchdog `task.cancel` skipped cleanup | `TrialKit.abortRoom(trialId, room)` + `module.abort()` on the legacy trio (module-level `_guard`); MachineFlow calls both after a cut |
| `BloodFX.kill` in `Round:kill` fired ALL clients | room audience passed; Minefield's kill/shatter now pass `audienceOf(room)` too |
| Compactor: 18-part model `PivotTo` at 20 Hz server-side | client-side march: server tags the model (`MF_Compactor`) and publishes `MF_Z0/MF_T0/MF_Speed` (+static X/Y/Yaw) ONLY on speed changes; every client integrates z against `GetServerTimeNow()` and pivots locally at render rate (`BindToRenderStep`, unbinds when no compactor). Procedural fallback keeps the server path |
| Turret re-pivot every 60 ms | pivot only when `aimDir` changed |
| 4 Humanoid jump writes per runner per 0.2 s | one-time per character (`st.jumpLockedChar`) |
| `GoreClient.refreshRayFilter` every Heartbeat | 0.5 s TTL |
| `MachineLayout.apply` per ViewportSize event | 0.15 s debounce (window drags fired it per frame) |

Left from the triage (low/later list unchanged): CanteenProps observer loop cadence, marquee/
screenBlood/flicker layer move, sfx()/machineState() lookups, kit.timer text writes, corpse collision,
countdown full-state ticks, canteen prop rebuild per round.
