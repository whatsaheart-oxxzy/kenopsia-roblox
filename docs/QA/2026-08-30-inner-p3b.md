# QA — Inner-Game Phase 3b: DEAD ZONE polish (30.08.2026)

Scope: INNER-GAME-PLAN.md §2.1 + phase 3 (DZ pass). 3-agent workflow (recon →
build → adversarial review); the first run's build/verify died on a session
limit and were resumed from the cached recon. Applied LIVE via weppy MCP
(placeId pinned; boundary read-backs verified line counts) and mirrored; all
touched scripts pass the Luau parser. Roblox_Studio proxy still unregistered —
live application via weppy, in-Play checks queued (list in APPLY.md §8, kept
with the phase-2/3a runtime debt).

## What shipped

- **Sonar economy:** the existing player-triggered `pulse` action (recon: full
  freshness gauntlet + 0.8s cooldown already live) now spends a SERVER budget
  of `Feel.Minefield.SonarPulses` (3) per runner per round. The reveal moved
  from the per-player `{kind="mines"}` packet to **server parts visible to
  all**: the authored MF_SonarRing grown to SonarRadius (12) + Neon glow discs
  (2.5s, extend-on-repulse) on live mines — the canteen's "the server's
  picture" doctrine. Counter/depletion via existing feed lines
  (`SONAR 2 LEFT.` / `SONAR SPENT.` once). KenopsiaClient's old hold-to-pulse
  (F/Y) keeps working and simply drains the same budget; its showMines handler
  just never receives packets (harmless).
- **NEW `SonarClient.client.luau`:** deliberate one-per-press E / gamepad
  ButtonX / own 72px touch button in its own ScreenGui (`DZSonarGui`), firing
  the existing TrialInput path with the token cached from role packets. Zero
  KenopsiaClient changes (register ceiling); gates on the live `XBotMoves`
  "scan" stamp (the `KenopsiaActiveTrial` player attribute is not stamped
  anywhere live — honored only if present).
- **Crater persistence:** detonations record positions in a module store and
  re-parent their MF_Crater clones into `DZ_Craters` (sibling of DZ_Runtime),
  standing ALL rounds of the trial; mine seeding excludes CraterClearRadius
  (6) around old scars — craters are proven ground. Feed line
  `THE FALLEN MARK THE PATH_` once per trial when scars first survive into a
  new round. Teardown: final round (Pacing.roundsFor), `abort()`, the
  thrown-round branch, a room-death catch in cleanup() (review fix #2), and a
  sessionId net at every round start. Review fix #1: crater clones are muted
  on adoption (Anchored, CanQuery/CanTouch false, all descendants — the
  MF_Compactor import pattern); CanCollide stays authored (walkable rim is
  design). No validateArena exists in Minefield — nothing to whitelist.
- **Mid-round speed step:** at RoundSeconds×SpeedStepAt (62×0.55≈34s) the
  compactor multiplies its phase speed by 1.12, published once through the
  existing MF_Z0/T0/Speed anchor (clients integrate seamlessly). Telegraphed
  CascadeSeconds=0.7 (= TelegraphMin) ahead: the 11 existing LaneLight parts
  flash Neon-red in sequence (EMISSIVE, never Light objects — the black-screen
  rule) with snapshot/restore that survives aborts, plus horn slot
  `SFX.Minefield.Horn` (created live, empty = silent + one warn).
- **BLACKOUT SWEEP (final round via Pacing.roundsFor):** pulses fire only
  after SonarStillTime (0.5s) of stillness measured from the 20Hz loop's OWN
  position samples (STILL_EPSILON 0.15/tick — walking rips it, physics jitter
  doesn't); throttled `HOLD STILL TO SCAN.` feed on refusal (free: no pulse,
  no cooldown spent); base sweep ×1.15 applied AFTER the clamp. Worst stack
  6.5×1.15×1.12 = 8.37 < ramped sprint 10 — escape on foot survives, walking
  (7) no longer does. Announced on GO: `BLACKOUT SWEEP. SCAN ONLY WHILE STILL.`
- **Scrap tags:** 3 Neon tags (TrialKit.PALETTE.Signal cyan, guarded lookup,
  hex fallback) seeded into the most-mined third, never on a mined cell;
  20Hz proximity pickup (2.5 studs, no .Touched), +150 detail each, Confirm
  blip via PlayOnRemove. Scoring: eliminated detail = progress + scrap clamped
  at DETAIL_CAP 999; proof min(1000+450,999)+1 = 1000 < ESCAPE_BAND 2000
  re-run by tests/feel.lua. Escapees get NO scrap detail (the CLEANRUN_BONUS
  ordering lesson).
- **Compactor death (E6):** verified no crush close-up exists (shatter + death
  card from second 0) — comment at the kill site, zero behavior change.
- **Config:** `Feel.Minefield` block (15 keys) inserted after Feel.Canteen;
  Pacing untouched (roundsFor/RoundSeconds already carry what's needed).

## Verification

- Adversarial review: 2 defects found + fixed (crater physics discipline;
  room-death teardown gap), 0 left unaddressed that matter (documented opens:
  mid-trial member-count change can skip the mutator — backstopped by three
  teardown nets; scrap placement math.random like the mines themselves).
  Reviewer re-ran its Lua 5.1 harness (20/20) and luau-analyze (lint set
  identical to live baseline).
- Offline from the real repo after sync: `tests/feel.lua` **168 checks, 0
  failures** (153 + 15 new), `tests/voice.lua` PASS, `tests/grade.lua` 54/0,
  `tests/rules.lua` PASS. (The patch's `check(name, cond)` arg order was
  flipped to the file's `check(cond, name, detail)` convention on apply.)
- Live: Minefield (1595 lines), FeelConfig (517), SonarClient (131) all
  weppy-validated clean; `SFX.Minefield` folder created; boundary reads exact.
- **PENDING in-Play** (APPLY.md §8, with the phase-2/3a debt): pulse budget +
  ring/glow visibility, crater survival into round 2 + feed line, ~t=33.4s
  cascade + speed step smoothness, blackout-round stillness gate, scrap
  pickup + band check. Needs the Roblox_Studio proxy (play + input injection).

## Notes

- Live sonar was ALREADY player-triggered with SCAN_RADIUS 8 — the plan's
  "replace automatic trigger" assumption was wrong in the happy direction;
  the economy is a budget bolted onto a proven input path, and the radius
  moved to Feel (12).
- roundIndex reached Minefield but was previously used only in the token
  string; the blackout/last-round derivation is its first gameplay use.
