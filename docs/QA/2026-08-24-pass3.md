# QA — 24.08. pass 3 (blackout, new clips, PS1 sky, canteen rework, boss sweep)

All live-checked solo unless noted.

1. **Round blackout** — every round ends with `{kind="cover"}`: the Fader cuts to black (~0.7 s) before
   the standings card / next round card, exactly the reference cadence. Measured: fader visible at
   round end for ~1.4 s incl. beat overlap.
2. **Three new clips retargeted** onto `Workspace.Anim_FieldClips` (same pipeline as the field clips):
   `Sneak` (86 kf, 2.83 s loop, natural 5.65 studs/s), `Dance` (96 kf loop), `RifleIdle` (94 kf loop,
   pose capture checked). Wired: Sneak REPLACES the crouch gait wherever loaded (Blender Crouch stays
   the fallback), Dance = G / DPadLeft / a small touch DANCE button (stands still, any movement
   cancels), RifleIdle plays on the hunter while idle (`XBotRifle`, cleared with the leg).
   `AnimationIds.Player.Sneak/Dance/RifleIdle = 0` + StudioSequences bridge (attributes verified);
   **user publishes the three from the Animation Editor on `Anim_FieldClips` under the group** and
   sends the ids.
3. **PS1 sky** — generated a 128 px Bayer-dithered light-blue gradient, uploaded as
   rbxassetid://131765931649272, and the birdhunt grade swaps the authored skybox for a flat Sky with
   that face on all six sides (restored after the trial). Edit-mode capture: chunky pixel sky, pale
   horizon band. DZ/canteen are indoor/night — unaffected.
4. **Canteen rework** — ONE pea per stab (server `take = min(1, space, remaining)`), fork caps at 4,
   the stab clip plays per click as before; a small red **EAT** label appears under the fork pips at
   4 (client). Live probe: fork 1→2→3 per click, mouth ate the load. EAT label untested at 4 solo
   (probe only landed 3 stabs) — logic reviewed.
5. **Boss scans both ways** — during `watching` the boss pivots −9° / +9° / centre in PS1 steps
   (0.42 s per hold), restored on any phase change. Live: yaw −90 → −99 → −81 → −90 on every
   watching phase.

Parity (9 pushes): AnimationIds 12868/2072654931, CanteenProtocol 37700/2143865907, CanteenBoss
13015/1742415243, BirdHunting 37792/350682026, MachineFlow 45401/1768214273, PS1Animate
10295/712485885, SimulationGrade 10441/790234953, KenopsiaClient 109339/1791787935. TrialIds
restored. Suites green.

Open: user publishes Sneak/Dance/RifleIdle ids; 2-player looks at the blackout pacing and the boss
sweep from a diner seat; EAT label at a full fork.
