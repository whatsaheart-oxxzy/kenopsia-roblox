# QA — Inner-Game Phase 4b: SHIFT REPORT beat + badge sockets (30.08.2026)

Scope: INNER-GAME-PLAN.md §3.1 + §6 (badges). Lean 2-agent workflow (build →
adversarial review; recon skipped — the Phase 4a staging was byte-identical to
live, verified before build). Review found ZERO defects. Applied LIVE via
weppy MCP as 9 minimal-edit hunks against content-verified anchors (bottom-up
per file) + one new ModuleScript; final line counts match the reviewed
artifacts exactly (MachineFlow 1415, Profiles 449, Pacing 190, Badges 134).
Mirrored; parser-clean.

## What shipped

- **`Pacing.Timing.ShiftReport = 5.0`** — the retention beat's length lives
  where every other beat does.
- **`Profiles.recordSession` now returns the receipt**: `{ xpDelta, xpTotal,
  clearance, clearanceName, clearanceUp, streak, sessions, viable,
  viableCount, bestBand, dailyFirst, volatile }` — computed from the same
  values it stores (zero drift), backward-safe for callers that ignore it.
- **MachineFlow — the SHIFT REPORT stage**: after the 6s verdict-stamp hold
  and before the podium, every participant gets a personal
  `{kind="shiftreport", ...receipt..., viable, openSeats}` packet on the
  tellRoom send shape, then the stage holds `Pacing.Timing.ShiftReport` (5s).
  **Dark but real**: the client renders nothing for the unknown kind yet, so
  until Codex renders it the beat reads as ~5 extra seconds on the final
  board — deliberate (the plan: "the BEAT is real"), tunable via the one
  Pacing number. Bench/late joiners get nothing. A quitter mid-beat ends the
  session as `completed` (scoresCommitted already true).
- **NEW `Services/Badges.luau`** — the six retention badges as a dormant
  socket (every id = 0 = silent skip, the CRT-hum pattern; **arming them is a
  Creator Hub badge creation + config paste, zero code change** — queued with
  the end-of-build user blockers). Rules: FIRST_SHIFT (sessions==1),
  FIRST_VIABLE (viable && viableCount==1), CLEAN_SWEEP (sole-topped every
  board, 2+ trials), THREE_NIGHTS (streak≥3, shield counts), FULL_ROSTER
  (judged roster of 4), CO_WORKERS (a Roblox friend seated). checkSession
  never yields (AwardBadge + the lazy IsFriendsWith both live on a spawned
  thread, gated on armed+unawarded so the dark socket spends zero API calls);
  server-lifetime per-user award guard; double-award harmless anyway.
- **MachineFlow — receipts + badges in the verdict loop**: recordSession's
  return captured per participant (pcall second value), Badges.checkSession
  called with the JUDGED roster count (a quitter during the holds can't
  unmake FULL_ROSTER), voice.trialWins, #order, and the other participants'
  ids for the lazy friend check.

## Verification

- Adversarial review: **0 defects** — verified the beat's placement/
  cancellation paths, receipt-vs-stored zero drift, clearanceUp across
  sessions, the id=0 silence, no yields on the verdict path, CLEAN_SWEEP
  cannot fire on 1-trial sessions or exceed trialsTotal, Pacing's Lua 5.1
  purity, and the no-duplicated-Timing guard.
- Offline from the real repo after sync: feel **190/0** (188 + ShiftReport
  check + the auto-added no-duplicates check), progression **116/0**
  (104 + 12 pinning clearanceFor's name return and display-safe band names),
  voice PASS, grade 54/0, rules PASS.
- Live: MachineFlow + Profiles weppy-validated clean; `Remote` upvalue
  confirmed in scope for the report sends; every hunk anchor content-verified
  before insert.
- **PENDING in-Play** (standing debt): the session's new ~5s beat visible as
  a held final board; quitting mid-beat recovers; console silent (no
  [Badges] warns with ids 0).

## Accepted risks

The dark 5s hold until the UI lands (deliberate); CLEAN_SWEEP is sole-top
(tie breaks the sweep — one-line context swap if the ruling meant tie-aware);
volatile servers re-fire FIRST_* rules once armed (Roblox no-ops owned
badges); armed CO_WORKERS costs ≤3 IsFriendsWith calls per participant per
verdict until first award.

## User actions queued (end-of-build blockers list)

Create the 6 badges in Creator Hub and paste their ids into
`Services/Badges.luau` → `Badges.BADGES` (FIRST_SHIFT, FIRST_VIABLE,
CLEAN_SWEEP, THREE_NIGHTS, FULL_ROSTER, CO_WORKERS).
