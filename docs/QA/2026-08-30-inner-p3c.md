# QA — Inner-Game Phase 3c: BIRD HUNTING polish (30.08.2026)

Scope: INNER-GAME-PLAN.md §2.2 + phase 3 (final trial pass — Phase 3 trial
polish is now COMPLETE across all three shipped trials). 3-agent workflow
(recon → build → adversarial review; the first run was stopped mid-recon on
user request and resumed cleanly). Applied LIVE via weppy MCP (placeId pinned,
boundary read-backs exact) and mirrored; all touched scripts pass the Luau
parser. In-Play checks queued with the standing runtime-test debt (APPLY.md
§ In-Play checklist).

## What shipped

- **Reload lockout + lamp:** `FIRE_COOLDOWN` now reads
  `Feel.Birdhunt.ReloadSeconds` (2.2) — the existing silent-reject server fire
  gate, unchanged in shape. NEW `BH_ReloadLamp`: a neon bar 3.4 studs over the
  SniperPost (in the arena folder, NOT the turret model, which PivotTos every
  0.06s) — amber while the bolt is open, Signal-cyan when loaded; readable at
  300 studs with sound off; the existing 0.45s SniperReload sound stays the
  audio half. **Review fix #1:** a generation counter guards the lamp's
  ready-flip against the `>` vs `>=` same-Heartbeat race with the next
  accepted shot — the lamp can never show ready during a live lockout.
  Hunter-side feedback: the monolith's **dead-since-Gate-1 `hitmark` handler
  finally receives packets** (TERMINATED on any terminal hit, IMPACT on
  wing/bird) — server-side sends only, zero client edits.
- **Runner injury:** `INJURED_SPEED` 7 → `Feel.Birdhunt.InjuredSpeed` (3.5) —
  a winged runner now limps at the speed InjuredWalk was authored for
  (3.49 st/s), making finish-or-switch a real hunter decision. Both write
  sites + the GO guard use it; heal stays the existing next-leg placement /
  cleanup restore.
- **Push-topple cover:** 1 push per runner per leg (server budget):
  the nearest untoppled `Metal_crate`/`Wooden_pallet` within 6 studs tips 85°
  away from the pusher over 0.5s (stepped PivotTo, hinged on the far bottom
  edge, parts anchored for the fallen phase), stays down as low cover, and
  stands back up at leg end from a pivot+Anchored snapshot (abort-safe: the
  topple task exits on `stopped` before any post-restore write). Input: NEW
  standalone `RunnerClient.client.luau` (SonarClient pattern; R / gamepad
  ButtonB / 72px touch button in its own `BHPushGui`), firing the shared
  `TrialInput` remote — BirdHunting.init grew its own filtered handler with a
  per-leg freshness token (`BH-<session>-L<leg>-<ms>`) carried on runner role
  packets. The Push clip is deliberately NOT played: PS1Animate is
  gait-attribute-only, and stamping "push" into XBotMoves would surface the
  dead legacy Btn_PUNCH → stranded XBotPush remote; the falling crate is the
  feedback. `XBotMoves` stays "sneak" (also the sender's arm gate — disjoint
  from DZ's "scan", so the two touch buttons share a screen slot safely).
- **Heartbeat exposure:** standing still >2.5s (server's own 0.2s samples,
  STILL_EPSILON scaled from Minefield's) in open ground (no cover-chart entry
  within 5 studs) → 0.4s red Highlight (AlwaysOnTop, first Highlight use in
  the codebase, ≤3 alive « the 31-instance cap), repeating every 1.5s.
  **Documented adaptation:** visible to ALL including the runner — a telegraph
  about you that you cannot see is not a telegraph. **Review fix #2:** a
  toppled prop counts as cover at its OLD and NEW position (the hinge
  displaces the resting pivot ~4 studs — most of CoverRadius — so a runner
  hugging their own fallen crate no longer reads as open ground).
- **Decoy birds:** 2 per leg, 3-part anchored dark models crossing the lane at
  14 st/s (never faster than a runner — lead learned on birds transfers),
  server-moved at 10Hz (one PivotTo per bird per tick; deliberately NOT the
  compactor attribute pattern — the shot raycast must see the bird at its true
  server position). CanQuery TRUE on bird parts (the one deliberate exception:
  being hittable is the decoy's job); hit = server ray + server-set `BHBird`
  attribute. Shooting one: +200 hunter detail, consumes the shot AND the
  reload (the bluff cost), sparks + ricochet (machine bird, machine feedback,
  never blood), tumbles 0.7s, despawns, never respawns.
- **Scoring:** hunter's one accumulator (tags + birds) clamped
  `math.min(points, 999)` (DETAIL_CAP pattern). **Review fix #3:** the proof
  was understated — the true worst case is wing (15) THEN headshot (100) per
  runner (double-wing impossible: the second body shot kills at 45 HP ≤ 55),
  so max = 3×115 + 2×200 = **745** ≤ 999; the test asserts the strengthened
  bound. Runner keys byte-identical.
- **Config:** `Feel.Birdhunt` block (15 keys) after Feel.Minefield; a stale
  "90s" comment corrected to the live 3×60s. Pacing/KenopsiaClient untouched.

## Verification

- Adversarial review: 4 defects found + FIXED (lamp/lockout race, toppled-
  cover chart gap, understated cap proof, one new luau-lsp finding); leg
  lifecycle proven across normal/abort/watchdog paths; exposure/birds/push all
  server-authoritative with no .Touched; reviewer ran the patched suites from
  a scratch repo: feel 188/0, grade 54/0, voice 27/0 PASS, rules 88/0 PASS;
  selene 0/0/0 on all three files; luau-lsp finding set identical to the live
  baseline.
- Offline from the real repo after sync: feel **188 checks, 0 failures**
  (168 + 20 new incl. the 2.2 K.FIRE_COOLDOWN pin and the 745-cap proof),
  voice PASS, grade 54/0, rules PASS.
- Live: BirdHunting (1582 lines), FeelConfig (525), RunnerClient (134) all
  weppy-validated clean; boundary reads exact.
- **PENDING in-Play** (APPLY.md checklist, with the standing debt): lamp
  cycle + hitmark first-fire, limp at 3.5, push topple + restore (incl.
  abort), exposure ping + cover exemptions, birds crossing/shot/despawn,
  Btn_PUNCH absence, SonarClient/RunnerClient cross-trial isolation.

## Notes

- The hitmark wiring resurrects a handler KenopsiaClient has carried unused
  since Gate 1 (its own comment said so); parity between the live monolith
  (4021 lines) and the repo copy was verified on the handler shape — one live
  hit confirms the text (checklist).
- Known couplings documented in APPLY.md: K.FIRE_COOLDOWN drift pinned by
  test; hunter-lost legs freeze pushes/lamp with `active=false` (existing
  semantics); ground decor counts as cover (acceptable slack); RunnerClient
  caches any runner-role token (harmless while "sneak" stays Bird-only).
