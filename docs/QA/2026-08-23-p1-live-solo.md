# QA — P0 leftovers + P1.3/P1.4/P1.7 live check, solo (23.08.2026, evening)

Branches of three offline agents merged (`f04a35c`, `8301513`, `d975a62` → `b75a019`), pushed to the
place with parity (11 scripts + the client fix below). Live check in Studio, one player, temporary
`TrialIds = { "birdhunt" }` (restored; GameConfig 4045/460735722).

## Found only live: the client stopped compiling

`KenopsiaClient:2451: Out of local registers when trying to allocate btn: exceeded limit 200` —
Luau allows 200 live locals per function; the P1 merge (podium camera state, voice labels, role card)
pushed the main chunk over it, so the whole Machine GUI logic was dead. Fix `a7e19a7`: 18 one-shot
constants moved into one table `K`, the look-panel labels scoped in a `do` block, 196 → 177 top-level
locals. `[Kenopsia] client online` again. Rule for every later client change: no net new top-level
locals (the FeelConfig brief carries it).

Also: two quick READY clicks toggle readiness on and off (`ReadyReq:FireServer(not myReady)`) — one
click per intent in tests.

## Session log (MachineState kinds, seconds)

`info` → READY → `info` (briefing) → `status line=nil` → `round` → `announce` (solo keeps the leg's own
hunter card) → leg 60 s → `status line=ONE SUBJECT KEEPS WINNING. RECALIBRATE.` → … → after leg 3
`score line=FULL SWEEP. THE SURVIVOR IS UNDER WATCH.` → `score FINAL line=OUTPUT BELOW TOLERANCE.
PROCEED ANYWAY.` → 6.1 s → `podium cam=true rows=1` → 8.1 s → `podium` clear → `hide` → `info`,
`selection` (rematch lobby).

* Standings and score cards carry a MachineVoice line (streak / sweep / outcome categories seen).
* Verdict stamp 6 s then podium packet for 8 s; `Podium.raise` on the server builds `PodiumStage`
  (camera marker (900, 8, 1522) → (900, 5, 1500)), anchors the winner's root on block 1 at
  (900, 9.56, 1500); `strike` removes the stage and frees the character (verified by direct call).
* Role card: by design only with ≥ 2 members (solo has no hunter) — 2-player check pending.
* Audience camera: needs a third client on the bench — pending.
* Chat hide / sfx warn / LobbyReveal 6.4 s: not observed separately this run (no chat window in the
  solo capture path); LobbyReveal reads `Pacing.Timing` (rules test).
* Podium capture through the live client failed: the client camera writer owns the camera every frame;
  take the picture via the real `podium` packet in the 2-player run.

## Place hygiene (P0.4, Studio, one ChangeHistory recording)

Duplicate lamps 02/06/07/08, duplicate props (Packaging/mre/trash/metal_shelf), three Mixamo archive
sources deleted; strays archived under `ServerStorage.KenopsiaAuthoring.Archive` (`WS_*`); loose
`bench` moved into the Bird Hunting folder. Workspace 3777 → 3602. Baseplate kept (lobby floor).

## Open for the 2-player test

Role card before each Bird Hunting leg (name once), voice lines with a real rank swing, podium with
two blocks lit, bench audience camera (third client), chat window hidden in a trial.
