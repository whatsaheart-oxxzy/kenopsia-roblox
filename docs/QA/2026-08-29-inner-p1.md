# QA — Inner-Game Phase 1: Repair & derisk (29.08.2026)

Scope: INNER-GAME-PLAN.md phase 1. All work applied LIVE to place 110672791536316 via
Studio MCP (studio 527862b7) and mirrored to `studio-src/`. Everything reversible:
moves went to archive folders, no deletes; old values recorded below.

## 1. Lighting repair (E4 blue baseline)

Measured BEFORE (damaged): Ambient (76,66,66), OutdoorAmbient (106,94,94),
Brightness 1.08, ClockTime 9.4, FogStart 823 / FogEnd 832 (razor band, one edit from
FogStart==FogEnd), FogColor ≈ (823,823,823)/255 HDR-white out of range, Exposure 0.

Applied AFTER: Ambient (38,46,66), OutdoorAmbient (56,68,94), Brightness 1.0,
ClockTime 17.6, FogColor (16,22,34), FogStart 40, FogEnd 480, Exposure 0,
GlobalShadows false, EnvironmentDiffuse/SpecularScale 0 (both unchanged).
FogEnd 480 preserves the ≥480-stud arena-spacing invisibility rule.
**This state is the grade-restore truth for Phase 2 GradeDirector snapshots.**

Verified: values re-read after write; canteen interior capture legible on ambient
alone; Bird Hunting 328-stud sniper→start sightline reads with partial fog
(per-trial grade brightens it in Phase 2). Reminder: Studio captures at Automatic
quality may drop local lights — captures used for composition only.

## 2. Sky twin (KenopsiaSky_PS1_Blue)

Regenerated from `Documents\Retro\Brutal Skyboxes\...\skybox_plain_emx_13.png`
(4096×1024 equirect) with the surviving 28.08 projection/quantisation script; blue
grade inserted pre-quantisation: desat 25 % toward luminance, gains R×0.78 G×0.88
B×1.18, 10 % shadow lift toward (16,22,34) to meet the new FogColor. RGB555 +
Bayer 4×4, 6× 256², ~152 KB. Script: `tools/blue_sky_twin.py`.

Uploaded (group place, via Studio MCP upload): Up 93350082828464,
Dn 91713728044014, Lf 81067778744933, Rt 107478028722796, Ft 108081155821657,
Bk 139991689514814. New `Lighting.KenopsiaSky_PS1_Blue` (CelestialBodiesShown
false, StarCount 0) is the only Sky; old `KenopsiaSky_PS1` moved to
`ServerStorage.KenopsiaAuthoring.Archive`.

## 3. F-16 lamp de-dupe (canteen)

Measured BEFORE: 4 FloorLamp models, each with **3 stacked duplicate `Lampe` parts**
(~0.45 studs apart on Z — the z-fighting), each part carrying PointLight +
SurfaceLight = 24 lights in lamps, only one SurfaceLight per lamp enabled; +1 stray
enabled PointLight (`CanteenProtocol.Light`) = 25 lights total.

Applied: per lamp kept the part with the enabled SurfaceLight; the 8 duplicate parts
moved to `ServerStorage.KenopsiaAuthoring.Archive.F16_LampDupes_20260829` (grouped
per lamp for undo). AFTER: 9 lights in the canteen (≤12 budget), z-fighting source
gone. F-16 CLOSED.

## 4. RunnerVsCars parked

`Workspace.RunnerVsCars` (596 descendants, 257 parts / 339 models, zero code
binding anywhere) moved to new `ServerStorage.KenopsiaArenasParked`. Frees render +
texture memory on every client; StreamingEnabled stays false per F-22.

## 5. New scripts (live + mirror)

- `Services/Telemetry.luau` — AnalyticsService skeleton: `Telemetry.event/funnel`
  (signatures verified against the current API dump: LogCustomEvent(player, name,
  value, customFields), LogFunnelStepEvent(player, funnelName, funnelSessionId,
  step, stepName)); client-report RemoteEvent `TelemetryEvent` (whitelist
  {MobileBlackScreen}, 60 s per-name rate limit, payload sanitizer). **Review-found
  HIGH fix:** LogCustomEvent accepts ONLY Enum.AnalyticsCustomFieldKeys
  CustomField01..03 — arbitrary keys are engine-rejected; payload is folded into
  "key=value" category strings on those keys. Started from `Main` directly after
  `RoomService.start()` (which creates the Remotes folder).
- `StarterPlayerScripts/LightGuard.client.luau` — mobile black-world sentinel.
  Engine fact: no pixel readback exists, so the "luminance self-test" from the plan
  is implemented as the honest heuristic: touch device AND
  UserGameSettings.SavedQualityLevel ≤ 4 (0=Automatic counts) → local ambient
  floor +(14,16,20), attribute `KenopsiaLowLight`, one MobileBlackScreen event
  (texMB bucketed to 25 MB so the analytics breakdown stays categorical). Desktop
  and failure paths are pure no-ops.
- `Shared/Config/AtlasTiers.luau` — tier table for the 512px twin program: top 40
  texture ids by usage (census below), all `low = nil` until twins are published;
  `lowFor(id)` helper; PlayerNeutral/BossNeutral rig atlases marked KEEP 1024.
  Consumer ships in Phase 2 GradeDirector.

## 6. Texture census

Full census in `docs/place/08-TEXTURE-CENSUS.md`: **115 unique texture assets, 913
references.** Per container: Dead Zone 556/54, Bird Hunting 239/34, Canteen 50/6,
MF_Shredder 16/6, KenopsiaAssets.Props 32/12, Rigs 10/2, SniperRifle 1/1,
StarterCharacter 3/1, Sky 6/6. Zero Decal/Texture instances; zero
metalness/roughness maps. 512-twin candidates = arena/prop mesh textures; the six
expression atlases stay 1024.

## 7. Smoke test

Play solo after all changes: `[Kenopsia] server ready (main game)`,
`[Kenopsia] client online`, MachineFlow online 3/15 ready, TelemetryEvent remote
present on the running server, zero errors from project scripts. (Console noise
observed: Roblox CorePackages ExpChat/UrlBuilder timeout — engine-internal, not
ours. AnimationIds used the RBX_ANIMSAVES Studio fallback as designed.)

## Deferred / gates

- Phone MicroProfiler capture with no black frame (issue #20) — **user device
  action**, per user directive queued with all release blockers after the build
  finishes.
- 512px twin uploads (AtlasTiers.low population) — Phase 2+, after the twin
  generation pass.
- RBX_ANIMSAVES purge from the published place — only after a live-server clip
  verification pass (needs a published update).
- Studio play banner: published animation ids still don't play in Studio sessions
  (fallback active) — live servers unaffected; group-permission Studio quirk.
