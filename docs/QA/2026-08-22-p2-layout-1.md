# QA — P2.1 (part 1): crosshair in scaled space, platform detection, geometry artefacts (2026-08-22)

**Place:** Kenopsia_MainGame, Studio Play Solo, desktop viewport 1268.8 × 576 (DPI 1.25), `[MachineLayout] Desktop, scale 0.81`.

## Roulette crosshair

The tile centre is an absolute screen point, but `CrossH`/`CrossV` live inside `Selection`, which MachineLayout scales (UIScale 0.81 on desktop): the old code applied the absolute offset as a local one, so the lines landed 0.81× too high/left (+16 px fudge). Now converted into Selection's own pixel space.
Measured after the snap tween: tile2 centre (634, 230), CrossV.x 632, CrossH.y 228 → **dx −2.4 px, dy −2.4 px** (before: ~59 px high).

## Platform detection

`detectPlatform()` composes `TouchEnabled / MouseEnabled / KeyboardEnabled / GamepadEnabled` with the viewport: gamepad-only → Console; touch with short axis < 500 → Mobile; touch with aspect < 1.6 → Tablet (iPad 4:3), else by short axis. Scale clamps: Mobile 0.55–0.85 (was 0.42), Tablet 0.7–1.0 (new), Desktop/Console unchanged. Tablet uses the Mobile controls text. `ForcePlatform` accepts "Tablet".

## Info screen (desktop)

No overlap at 0.81: TopTicker (165, −37) 13 px, NextLabel (165, −21) 29 px high, SelIcon (12, −34) 119 × 114, ControlsWindow (165, 36) 578 × 317, Roster (767, 31), READY (774, 277) 482 × 65. The user's overlap screenshot must come from a phone-sized viewport — to be measured with the device emulator (part 2).
Artefacts fixed in StarterGui: `Info.Grunge.Right` X scale 1.0027 → {1, −90}; `Btn_SETTINGS` negative Y scale → {1, −16},{0, 56}.

## Gate

- [x] crosshair within 3 px of the tile centre
- [x] parity place ↔ studio-src: KenopsiaClient, MachineLayout
- [ ] part 2 (needs the device emulator, user + me): phone 19.5:9, iPad 4:3, console; measure overlap, 44 pt touch floor, READY reachability

## Part 2 (phone emulation, 749 × 368 landscape, `Mobile`)

- Crosshair: dx −1.3 px, dy −1.4 px at scale 0.55 ✓.
- Before the text floor: **16 Info labels rendered < 11 px** (13-px ticker → 7 px, 17-px control rows → 9 px) — this is the "status line glued to NEXT SIMULATION" from the user's screenshot.
- Fix: `MachineLayout.applyTextFloor` (every label keeps `AuthoredTextSize`, rendered size ≥ 10 px) + mobile scale floor 0.55 → 0.62. After: smallest rendered label 10.5 px, 0 below 10; NavCluster bottom 356 of 368, READY 285 × 44 → 297 px bottom; touch buttons FIRE 104², SCOPE 76² (≥ 44 pt).
- The text column (`TopTicker`, `NextLabel`, `ControlsWindow`) is authored at x = 0.13, which at this width lands under the SelIcon brackets; now `max(0.13 × width, 174 px local)` → icon→text gap 7 px, controls→roster gap 4 px.
- Desktop (1268 × 576, 0.81) unaffected by the floor (13 px ≥ 10 px rendered).

## Part 3 (iPad emulation, 1178 × 818, 4:3 → `Tablet`, scale 1.0)

- Detection: `Tablet` (touch, aspect 1.44 < 1.6), scale clamp 0.7–1.0 → 1.0; Mobile controls text; smallest label 13 px; crosshair dx −2.4 / dy −2.0.
- Defect found: the CONTROLS window (authored 45.5 % wide, now starting at the 174-px column) ended at 711 px while the Roster starts at 712 — its border cut through the roster checkboxes. Fix: `MachineLayout` clamps the window width to `rosterX − col − 16` (authored width scale remembered in `AuthoredWidthScale`, floor 200 px). After: 522 × 451, gap 16 px, pager arrows inside the frame (686 < 696).
- Touch controls are hidden in the lobby on every platform by design; in-trial pads unaffected.

## Part 4 (Xbox emulation, 1918 × 1080 → `Console`, scale 1.40)

- Detection via `IsTenFootInterface` (emulator reports gamepad=false, mouse/keyboard true — the ten-foot flag decides). Console controls text (`LS Move`, `B Crouch`, `RS Aim + RT Shoot`), A-glyph on READY, `GuiService.SelectedObject = Btn_READY` on entry.
- Smallest label was 18.2 px (13 × 1.4); console floor `MIN_TEXT_PX_CONSOLE = 22` raises it to 22.4 px (one label raised, the ticker; 5 px to the title).
- Focus ring: `PlayerGui.SelectionImageObject` = `KenopsiaFocusRing` (phosphor 3-px stroke + four corner ticks, 6 px outside the control) replaces Roblox's blue rectangle. Verified selected on READY.
- Layout: controls → roster gap 38 px, nothing over the edge. Real gamepad navigation (SelectionGroup traps, D-pad walk) still to be tested with a controller — the emulator has no gamepad input.
