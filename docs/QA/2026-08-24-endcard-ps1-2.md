# QA — end card, board-only-at-the-end, PS1 pass 2, full-session run (24.08.2026)

## Full-session QA (#37 scope, solo, real playlist)
One complete session S1 (DZ 4 rounds → BH 3 legs → CP 3 rounds → FINAL board → verdict → podium →
rematch re-armed → S2 started by itself). Console: ZERO errors/warnings beyond the known hum
placeholder. All four DZ deaths took the new direct-hit path (maim + legless). CP round 3 ended with
`finished=1`.

## New in this pass (commit c7e092e + direct-hit halving in c287f3b's follow-up)
- Direct mine hit also takes the lower half (maim + `XBotLegless` on the corpse; the field death
  plays with the legs folded under the floor and the hip clot).
- The winner board (odometer, verdict) shows ONCE, after minigame 3. Between minigames a 2.5 s rank
  card (`status` lines) keeps the pace.
- SHIFT COMPLETE end card after the podium: grunge slanted bars (reference: the user's B/W pause-menu
  shot), final top-4 with score + verdict, PLAY AGAIN / BACK TO MENU with hover inversion. PLAY AGAIN
  readies up (carried rematch does the rest); BACK TO MENU un-readies AND drops the player from the
  pending rematch set (RoomService). Verified live with synthetic packets: card renders (capture),
  BACK TO MENU delivered `ready=false`.
- PS1 pass 2: trials disable GlobalShadows, EnvironmentDiffuse/SpecularScale and any Bloom/DoF/
  SunRays effect (snapshot-restored); the camera HOLDS a 30 Hz cadence while the filter runs
  (`KenopsiaCamStep`, off for the birdhunt scope / ReducedMotion / SIM FILTER off).

## Honest limits (the user asked for "authentic PS1"; Roblox has no render hooks)
- TRUE low-res downsampling and indexed palettes are impossible (no post-process render target;
  ViewportFrame re-rendering the world is unlit and unaffordable). Substitute: grade + Bayer world
  grain + scanlines + 6 px camera quantisation + 30 Hz cadence.
- Vertex wobble / affine warp is impossible (no vertex shader access). The camera quantisation is
  the closest available analogue.
- Per-texture nearest-neighbour filtering is not exposed for MeshParts; our textures are authored
  low-res (128 px atlases, 1024 nearest-upscaled uploads) which delivers the look.

Parity: MachineFlow 45182/647723946, RoomService 20948/746709236, SimulationGrade 9359/499593119,
KenopsiaClient 108363/752680604 (179 locals). All 10 suites green.

Open: user judges the 30 Hz cadence + hard-light look on real hardware (SIM FILTER off in Settings
if it is too much); 2-player end card (both choices at once); restyle of the remaining lobby screens
onto the grunge bars is optional post-release polish.
