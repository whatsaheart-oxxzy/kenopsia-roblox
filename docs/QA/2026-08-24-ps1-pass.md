# QA — PS1 pass after the user's 24.08. requests

Four requests, all live-checked solo (Studio):

1. **"Real PS1 graphics in every trial"** — grade presets hardened (minefield fog 40/130 + sat −0.3 /
   contrast 0.15; canteen sat −0.45 / contrast 0.18; birdhunt sat −0.35 / contrast 0.1, density up),
   camera position quantisation 8 → 6 px, and a new full-screen ordered-dither GRAIN (Bayer level 2,
   8 px tile, alpha 0.93) over the world while a trial runs (`UiFx.TrialGrain`, off with CRT toggle).
   Live: grade + grain active in the DZ round (capture black-heavy due to the known Automatic-quality
   lamp artifact in captures; values verified via Lighting reads).
2. **Walk→run frame-matched, 1 s ramp in the Dead Zone** — `PS1Animate.play()` carries the normalised
   cycle phase across Walk↔Run crossfades (same-foot landing); DZ runners accelerate 7 → 10 studs/s
   over 1 s of continuous movement (client Heartbeat ramp; resets on stop/scan/crawl; server
   `MAX_RUNNER_SPEED` 7 → 10). Live: ws 7.2 → 10.0 across 1 s, Run gait takes over at ~0.75 s with
   the published Run clip.
3. **One mine takes the LOWER HALF** — the knock now sets `XBotLegless` and fires `BloodFX.maim`
   (hip gibs + the existing crawl blood trail; scope-safe). The rig is one skinned mesh, so
   `PS1Animate` pushes both leg chains 3.2×scale studs straight down every RenderStepped frame
   (after the Animator) — the arena floor swallows them — and parks a dark clot part with a drip
   emitter on the pelvis bone (client-local). Cleared wherever `XBotCrawl` clears. Live capture:
   prone crawl, body ends at the hips in the clot, blood splats trail behind, no legs.
4. **UI pass** — Selection tiles now carry designations: after the lock the chosen tile shows the
   trial's `displayName` in phosphor (selection packet carries `name`), the two losers dim to ghosts
   ("SIM 0n - HELD"); plus the trial grain + the stronger grade carry the in-game look.

Pushed with parity (PS1Animate 8634/1083909136, Minefield 41458/1052964591, BloodFX 7033/1197245157,
MachineFlow 44745/1408696820, SimulationGrade 8415/2024595388, UiFx 23228/1982794083, KenopsiaClient
103406/1811014213). TrialIds restored. All 10 offline suites green; KenopsiaClient still 178 locals.

Open: 2-player check of the maim visual from the second screen; the user judges the new grade
brightness on real hardware (captures cannot show local lights).
